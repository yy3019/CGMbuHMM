% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/hmm.R
\name{fit_hmm_cgm}
\alias{fit_hmm_cgm}
\title{Fit the beta-uniform HMM to one CGM series}
\usage{
fit_hmm_cgm(
  glucose,
  time,
  templates,
  stratum = NULL,
  max_gap_minutes = 6,
  step_minutes = 5,
  max_iter = 50L,
  tolerance = 1e-04,
  initial_probabilities = NULL,
  transition_matrix = NULL,
  prevalence_weight = TRUE,
  return_posterior = FALSE
)
}
\arguments{
\item{glucose}{Numeric glucose measurements.}

\item{time}{Observation times. Numeric values are interpreted as minutes.}

\item{templates}{Emission templates returned by fit_emission_templates, or
one cgm_emission_template.}

\item{stratum}{Stratum identifying which template to use.}

\item{max_gap_minutes}{Largest gap retained within a continuous segment.}

\item{step_minutes}{Minutes represented by one Markov transition.}

\item{max_iter}{Maximum Baum-Welch iterations.}

\item{tolerance}{Absolute scaled log-likelihood convergence tolerance.}

\item{initial_probabilities}{Optional initial state distribution.}

\item{transition_matrix}{Optional initial transition matrix.}

\item{prevalence_weight}{Apply baseline prevalence weights in overlaps.}

\item{return_posterior}{Return observation-level posterior probabilities and
decoded states.}
}
\value{
A cgm_hmm_fit object containing the transition matrix, initial
probabilities, MFPT values, convergence information, and optional
observation-level posterior probabilities.
}
\description{
The emission template is held fixed while the common segment-start
distribution and transition matrix are estimated by the Baum-Welch
expectation-maximization algorithm. Gaps longer than max_gap_minutes start
new segments, so transitions are not counted across those gaps.
}
\examples{
data <- simulate_cgm_data(n_participants = 4, n_per_period = 96, seed = 2)
baseline <- data[data$period == "baseline", ]
templates <- fit_emission_templates(
  baseline,
  stratum = "age_group",
  cutoffs = list(adults = c(70, 180), children = c(70, 180)),
  min_state_n = 5
)
one <- data[data$participant_id == "P001" & data$period == "rct", ]
fit_hmm_cgm(
  one$glucose,
  one$timestamp,
  templates,
  stratum = one$age_group[1]
)
}
