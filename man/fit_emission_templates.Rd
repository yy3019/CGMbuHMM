% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/emissions.R
\name{fit_emission_templates}
\alias{fit_emission_templates}
\title{Fit pooled beta-uniform emission templates}
\usage{
fit_emission_templates(
  data,
  glucose = "glucose",
  stratum = NULL,
  cutoffs = c(70, 180),
  domain = c(40, 401),
  overlap = 10,
  epsilon = 1e-06,
  boundary_nudge = 0.5,
  shape_min = matrix(c(0.2, 1, 0.8, 0.8, 0.8, 0.8), nrow = 3, byrow = TRUE, dimnames =
    list(.state_names, c("alpha", "beta"))),
  max_fit_n = 200000L,
  min_state_n = 20L,
  seed = 1L
)
}
\arguments{
\item{data}{Reference data frame, usually pooled baseline CGM observations.}

\item{glucose}{Name of the numeric glucose column.}

\item{stratum}{Optional name of a column defining separate templates, such
as an adult/child indicator.}

\item{cutoffs}{Two increasing cutoffs or a named list of cutoff pairs
indexed by stratum.}

\item{domain}{Admissible glucose domain. Values at or outside its endpoints
are excluded.}

\item{overlap}{Half-width in mg/dL added to each side of an adjacent-state
boundary.}

\item{epsilon}{Numerical boundary adjustment on the unit interval.}

\item{boundary_nudge}{Amount in mg/dL used to move exact support endpoints
into the support before beta-density evaluation.}

\item{shape_min}{Three by two matrix of lower bounds for alpha and beta.}

\item{max_fit_n}{Maximum observations used to optimize each state density.}

\item{min_state_n}{Minimum reference observations required in each state.}

\item{seed}{Seed used only when a state is subsampled for fitting.}
}
\value{
A cgm_emission_templates object containing one template per stratum.
}
\description{
Fits one beta-uniform mixture density for each of three ordered CGM states.
Observations are assigned to states using hard cutoffs for template fitting,
while the fitted supports extend into adjacent states by overlap mg/dL.
State prevalences in the reference data are retained for overlap weighting.
}
\examples{
data <- simulate_cgm_data(n_participants = 6, n_per_period = 96, seed = 1)
baseline <- data[data$period == "baseline", ]
templates <- fit_emission_templates(
  baseline,
  glucose = "glucose",
  stratum = "age_group",
  cutoffs = list(adults = c(70, 180), children = c(70, 180)),
  max_fit_n = 5000,
  min_state_n = 5
)
templates
}
