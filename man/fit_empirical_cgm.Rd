% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/empirical.R
\name{fit_empirical_cgm}
\alias{fit_empirical_cgm}
\title{Fit an empirical three-state CGM transition model}
\usage{
fit_empirical_cgm(
  glucose,
  time,
  cutoffs = c(70, 180),
  domain = c(40, 401),
  reference_transition = NULL,
  prior_strength = 0,
  max_gap_minutes = 6,
  step_minutes = 5,
  zero_row = c("self", "uniform", "error"),
  return_states = FALSE
)
}
\arguments{
\item{glucose}{Numeric glucose measurements.}

\item{time}{Observation times. Numeric values are interpreted as minutes.}

\item{cutoffs}{Two increasing state boundaries. The clinical default is
70 and 180 mg/dL.}

\item{domain}{Admissible glucose domain. Values at or outside its endpoints
are excluded.}

\item{reference_transition}{Optional three by three row-stochastic reference
transition matrix.}

\item{prior_strength}{Non-negative pseudocount total added to each row of
the observed transition counts. Requires reference_transition when
positive.}

\item{max_gap_minutes}{Largest gap retained within a continuous segment.}

\item{step_minutes}{Minutes represented by one Markov transition.}

\item{zero_row}{How to handle a row with no observed or prior transitions.}

\item{return_states}{Return the ordered observations and assigned states.}
}
\value{
A cgm_empirical_fit object containing counts, the transition matrix,
and MFPT values.
}
\description{
Glucose measurements are assigned to lower, middle, and upper states using
hard cutoffs. The transition matrix is estimated from consecutive
observations within continuous segments. Sparse transition counts can be
stabilized by adding a user-supplied reference transition matrix as
pseudocounts.
}
\examples{
fit_empirical_cgm(
  glucose = c(65, 72, 110, 190, 175, 140),
  time = seq(0, 25, by = 5)
)
}
